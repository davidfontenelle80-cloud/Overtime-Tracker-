# v23 — Audit fix (app-sweep 3/4)

Date: 2026-07-01
Scope: static audit per sweep method. No new features.

## Audit results
- JS syntax (`node --check`): all files clean.
- i18n: app is EN-only (no data-i18n usage, no EN/ES dict) — nothing to check.
- Service worker: all 20 precache entries exist; all 18 index.html asset refs precached; no cache drift.
- Theme selectors: main.css pairs `[data-theme="light"] body` with legacy `body.light` — theme.js sets data-theme on <html>, so both paths work. No bug.
- Spacing tokens `--space-lg/--space-xl` undefined but used only with sane px fallbacks in responsive.css — no visual break, left alone.
- Orphaned files: `js/auth.js` is KHub boilerplate, feature-flagged off — intentional.

## Bug fixed (index.html only)
- **Settings back button unstyled.** Inline style used `var(--accent)` (no fallback) for text + border, but `--accent` is never defined in this app — its accent token is `--ot` (components.css: `--khub-accent: var(--accent, var(--ot, #58c18f))`). The undefined var made color/border fall back to inherited text color instead of the green accent. Fixed to `var(--ot)`.

## SW
- CACHE_VERSION: overtime-tracker-v22-settings-back-adjust → overtime-tracker-v23-token-fix.
